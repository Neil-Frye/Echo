{
  "name": "@ethernalecho/content-service",
  "version": "0.1.0",
  "private": true,
  "description": "Content management service for EthernalEcho",
  "main": "index.js",
  "scripts": {
    "dev": "echo \"Content service development server\" && exit 0",
    "build": "echo \"Build content service\" && exit 0",
    "test": "echo \"Run content service tests\" && exit 0",
    "start": "echo \"Start content service\" && exit 0"
  },
  "dependencies": {
    "@ethernalecho/shared": "^0.1.0"
  }
}