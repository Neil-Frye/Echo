{
  "name": "@ethernalecho/billing-service",
  "version": "0.1.0",
  "private": true,
  "description": "Billing service for EthernalEcho",
  "main": "index.js",
  "scripts": {
    "dev": "echo \"Billing service development server\" && exit 0",
    "build": "echo \"Build billing service\" && exit 0",
    "test": "echo \"Run billing service tests\" && exit 0",
    "start": "echo \"Start billing service\" && exit 0"
  },
  "dependencies": {
    "@ethernalecho/shared": "^0.1.0"
  }
}