{
  "name": "@ethernalecho/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared utilities and types for EthernalEcho",
  "main": "index.js",
  "scripts": {
    "build": "echo \"Build shared package\" && exit 0",
    "test": "echo \"Run shared tests\" && exit 0"
  }
}