{
  "name": "@ethernalecho/ui",
  "version": "0.1.0",
  "private": true,
  "description": "UI components for EthernalEcho",
  "main": "index.js",
  "scripts": {
    "build": "echo \"Build UI package\" && exit 0",
    "test": "echo \"Run UI tests\" && exit 0"
  },
  "dependencies": {
    "@ethernalecho/shared": "^0.1.0"
  }
}